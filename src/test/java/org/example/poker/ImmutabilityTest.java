package org.example.poker;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.*;

@DisplayName("Task 5: Immutability Tests")
class ImmutabilityTest {

    // TODO: Task 5 - Implement immutability tests
    //
    // Acceptance Criteria:
    // - Given a Hand, when attempting to modify the cards list via getCards().clear(), then throw UnsupportedOperationException
    // - Given a Hand, when attempting to add a card via getCards().add(), then throw UnsupportedOperationException
    // - Given a Hand, when attempting to remove a card via getCards().remove(), then throw UnsupportedOperationException
    //
    // Hint: Use assertThatThrownBy() to verify exceptions are thrown
    // Hint: After writing the tests, fix the Hand.getCards() method to return an unmodifiable list
    //
    // Write your tests here:

}
